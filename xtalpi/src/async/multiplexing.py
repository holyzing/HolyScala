# -*- encoding: utf-8 -*-

"""
 IO 多路复用：

    |应用层 （socket Client）
    |   socket 是介于 应用层与运输层之间的抽象接口，它抽象了应用和TCP协议镞之间的交流，对外简化了它们之间通信的步骤
    |运输层（TCP/UDP 协议层）
    |网络层（ICMP IP ICMP）
    |链路层（ARP 硬件接口 RARP）

1 - 使用 cgroups 和 namespace 实现进程间隔离
2 - 进程间通信

应用层
表示层
会话层
传输层
网络层
链路层
物理层

"""


